npm install @supabase/supabase-js react-native-encrypted-storage react-native-url-polyfill

cd ios && pod install

-- setup the Supabase project

-- execute SQL script

-- set redirect URL for template email_url

-- go over project structure

-- create supabase client in lib/supabase

-- complete context/Auth

-- try to create an account and login

-- check for the new account in Profiles table

-- complete hooks/useProfile

-- try to update username

-- logout and log back in

-- show THE GAME

-- init GH repo and setup remote repo

    git init
    git add .
    git commit -m "initial commit for the best app ever"
    git remote add origin https://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@github.com/MetodiYanev/DevTalks.git
    git branch -M main
    git push -u origin main

-- goto AppCenter and add new app

-- Android build config ENV_VAR - name JAVA_HOME value $(JAVA_HOME_11_X64)
